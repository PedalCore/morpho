# M4 — memory binding and retrieval (formalization before training)

The probes and generations localize CRSA's failure precisely: forgetting
by COLLISION, not by time — perfect induction at every delay, weak
selective retention under distractors (0.56 vs 0.91), name drift in
generated stories. M4 is the repair program. Three problems at three
different stages of the block; they are NOT one problem:

| problem | stage | repair |
|---|---|---|
| insufficient nonlinear capacity | feature extraction | active overcomplete dictionary — **already substantially repaired** (8.40 vs 9.72) |
| entity collisions | writing history | separate information into keyed slots/statistics |
| weak selective retrieval | reading history | make the read depend on the current query |

## Why the counters collide

c_t = rho c_{t-1} + h_t^2. Squaring discards SIGN; summation discards
OWNERSHIP. If Tom and Tim activate overlapping coordinates, their energy
enters the same counters: the model knows an entity-like feature was
recently active, but not which entity owned which attributes. Write
collision (entities share statistics) and read ambiguity (no query can
ask for "Tom's information") are linked but distinct. Longer contexts
can WORSEN this — more entities through the same fixed statistics.

## The ladder (in order; one change per rung; zero-init contract)

Every new mechanism enters as a ZERO-INITIALIZED residual on the trained
parent (beta = 0 / gamma = 0 reproduces the parent exactly — verified
numerically at implementation time, 0.00 diff on the parent checkpoint).
No conversion shock; warm-start via strict=False.

**Rung 1 — signed moments (cheapest information-loss ablation).**
s_t = rho s_{t-1} + h_t alongside c_t; read dcoef*(h + beta*s).
State 448 -> 896 values/layer; +12 scalar params. Restores the sign
information h^2 destroys. Will NOT solve binding (Tom and Tim still
superpose); measures how much of the failure is the squaring loss alone.

**Rung 2 — local-cache retrieval ORACLE (diagnostic, not a design).**
y = y_CRSA + gamma * y_local, exact softmax attention over the last
W = 16 tokens only. State 2Wd = 14,336 values/layer (a W-token ring) —
fixed in total context length but much larger than 448 counters, and no
longer counters-only. Interpretation map:
- distractor recall + names + ppl improve => selective retrieval is the
  missing mechanism (proceed to rung 3);
- no improvement => problem is upstream feature representation;
- only recent references improve => cache fixes locality, not long-term
  entity memory.

**Rung 3 — sparse representative memory (the derived repair).**
M content-addressable statistical slots per layer. Sparse router
a_t = prox(q_t^T K) assigns each event to 1-2 representatives; each slot
keeps decaying statistics N_j = rho_j N_j + a_j, V_j = rho_j V_j + a_j v_t;
the read is r_t = sum_j pi_j(q_t) V_j / (N_j + eps). Tom routes to one
slot, the red ball to another; attributes accumulate separately; a
Tom-query reads Tom's slot, not the global mixture. Router as binary/WTA
prox with a fixed access-energy penalty — objective-derived,
spike-compatible, fixed work per token. State M d = 3,584 (values only)
or 2Md = 7,168 (dynamic keys) per layer at M = 8 — fixed whether the
sequence has 100 or 100,000 events. Implement ONLY if the oracle fires.

**Dictionary extensions (two ISTA steps, group-sparse atoms, larger q,
split key/value dictionaries): NOT NOW.** The factorial and the 20k run
say the feature block works; probes point at binding/retrieval.

## The probe-first evaluation ladder (collaborator protocol, adopted)

No 20k TinyStories run per repair. Tiny models (d=128, L=4, ctx=128,
matched budgets, the locked optimizer recipe) against five arms: CRSA |
signed counters | CRSA + local cache | CRSA + representative slots |
full KV as ceiling. Ladder: tiny synthetic probes (3 seeds preferred)
-> only passing mechanisms get a 3,000-step TinyStories screen -> only
screens with improved retrieval AND credible ppl trajectory get a long
run -> generation samples LAST, as qualitative confirmation. The
nonlinear-capacity question needs no new synthetic campaign (the
factorial closed it); these probes focus narrowly on binding collisions
and selective retrieval.

## The binding-swap probe (m4_probes.py — the sharp new instrument)

Matched twin pairs: STORE k1 v1 ... kF vF, identical names, attributes,
gap and distractor positions; ONLY the pairing permutation differs, and
the query targets a swapped position so the twins' answers must differ.
A marginal-statistics memory sees nearly the same evidence in both; a
binding-capable memory must separate them. Names/attributes randomized
every example. Axes swept INDEPENDENTLY to separate failure modes:

| sweep | held fixed | falling accuracy means |
|---|---|---|
| delay 8..96 (NULL fillers: pure time) | 2 facts | temporal forgetting |
| facts 2..16 | gap 16 | interference / collision |
| distractors 0..64 FILL (total gap fixed at 64) | 4 facts | weak selective retrieval |
| query gap 8 vs 48 (around W=16) | 4 facts | local-cache benefit vs persistent retrieval |

## Preregistered micro-gates (locked 2026-08-22, before any repair trains)

A repair earns a TinyStories screen ONLY if it:
1. preserves induction near 1.00 on the locked grid;
2. recovers >= half the CRSA-to-KV selective-recall gap:
   0.56 + (0.91-0.56)/2 = **0.735** minimum;
3. improves binding-swap accuracy AS STORED ENTITIES INCREASE
   (the facts sweep — beats parent CRSA at every level, gap growing);
4. beats parent OUTSIDE the cache window if it claims long-term
   retrieval (bind-window@48);
5. stays inside its declared recurrent-state budget
   (sm: 896/layer; cache16: 14,336/layer; slots M=8: 3,584-7,168).

Training distribution note: M4 probe models train on locked tasks +
binding mixed (m4_probes.train_stream) — a DIFFERENT distribution from
the locked M3 runs; arm comparisons are within-protocol, plus the
absolute induction gate.

## Judgment axes (all rungs)

Ordinary 256-token perplexity | selective recall under distractors |
associative recall | controlled name/object consistency | recurrent
state + hardware cost. A rung that buys ppl but explodes state is a
different architecture, not a repair.

## The long-context axes (SEPARATE from binding — three different numbers)

- **T** (training sequence length), **P** (position range), **H**
  (counter horizon) are independent knobs. Practical constraint with a
  learned absolute table: P >= T. P > T costs parameters without
  teaching long dependencies; P < T recreates the clamp defect.
- Position table cost is P x d static params (P=1,024 at d=448: +344k,
  +2.5% on 14M) — no recurrent-state or per-token-complexity change.
  Untrained new entries at inference would HURT; extension requires
  training. Cleanest control: parameter-free sinusoidal features
  (also removes the existing 114,688-param table). For MIDI later:
  elapsed ticks + cyclic beat/bar phase, no absolute position at all.
- Horizon: the ladder m in {3,4,5,6} x2 is mostly short-range. The
  8 heads can be REDISTRIBUTED to m in {3..10} (half-lives ~5 to ~710
  tokens) at the SAME 448 counters and ~same params — reallocating
  counter capacity from short-term detail to long-term history.
- The clean experiment: P=1,024 (or sinusoidal), extend the decay
  ladder, train progressively 256 -> 512 -> 1,024 on GENUINE continuous
  stories (concatenating unrelated stories adds irrelevant history and
  can worsen collisions), judge on ordinary ppl AND distance-conditioned
  entity retention separately — long-context gains may not move 256-tok
  ppl at all. This separates "position 256 was broken" from "the model
  learned longer memory".
- Note: the recurrent sampler (recurrent.py) clamps position beyond ctx
  — counters integrate unboundedly, the positional table does not.

## Expected shape of the result

The likely final architecture: sparse nonlinear features -> multiscale
counters + small sparse representative memory -> query-selective read.
CRSA keeps the efficient global "feel"; a small derived mechanism
remembers exactly who or what produced it.

## Status

- Rungs 1 and 2 implemented (model.py: CRSA.beta, CacheCRSA;
  train.py: --signed-moment, --local-cache W). Zero-init equivalence
  verified against the plateau-dict4local parent: 0.00 max diff, new
  params receive gradient, warm-start loads with strict=False
  (signed: +12 params; oracle: +2.4M for the local U projections).
- Sequencing: finish the current floor run (plateau-dict-ext20k), then
  rung 1 and rung 2 from the best checkpoint.
- Rung 3 formalized above; implementation gated on the oracle verdict.


## PROTOCOL AMENDMENT (2026-08-22, recorded after the kv arm, BEFORE
## any repair arm reported)

Instrument-validation finding from the first sweep arm: tied-projection
MSSA ("kv") sits at EXACT CHANCE on every binding cell (0.50/0.25/
0.125/0.0625 at facts 2/4/8/16) while holding induction 0.99-1.00 and
selective 0.72-0.76. Re-reading the locked suite confirms this was
always true: "assoc weaker" was in fact assoc ~ stored-set chance for
EVERY arm including full-window MSSA (~0.30 at pairs=4, chance 0.25).
Mechanism: Q=K=V through one shared projection cannot easily learn the
attend-to-token-AFTER-the-matching-key composition that key->value
binding needs at this scale. Consequence: MSSA provides NO binding
ceiling, and gates 3-4 were uninterpretable as preregistered.

Amendments (both recorded before crsa/crsa-sm/cache16 results existed):
1. A TRUE ceiling arm added: `qkv` — standard untied multi-head
   attention (separate Q/K/V/O, not white-box), appended to the sweep.
   Binding gates 3-4 are referenced against qkv, not MSSA. If qkv also
   fails binding at this scale, the task needs curriculum/capacity
   changes before any repair verdicts are read.
2. Gate 2's absolute threshold (0.735) was derived from the LOCKED
   runs' training distribution; the M4 mixed stream shifts absolute
   numbers (kv selective here: 0.72-0.76 vs 0.91 locked). The gate is
   recomputed within-protocol with the same formula: threshold =
   crsa + (ceiling - crsa)/2, using this protocol's own crsa and best
   ceiling (kv or qkv) selective means.
3. The locked-suite reinterpretation (assoc ~ chance everywhere) is a
   FINDING to carry into PROBES.md conclusions: the M3 scorecard's
   assoc row should not be read as "partial capability" for any arm.


## REFRAMING (collaborator, 2026-08-22, after the kv chance finding +
## rung-1 fail; before cache16/qkv reported)

The repair target sharpens: the architecture may lack a mechanism that
separates an ADDRESS (key), a LOOKUP QUERY, and the VALUE stored at
that address. CRSA compresses h^2 into statistics; tied MSSA uses ONE
projection for all three roles. Both preserve activity and repeated
patterns; neither necessarily forms arbitrary key->value bindings.
"Collision" may describe TWO different failures:
1. multiple memories compressed into the same state;
2. the model never learned an address->value binding circuit at all.
M4 is now distinguishing those.

Updated interpretation per repair:
| repair | updated reading |
|---|---|
| signed moments | cheap diagnostic for squaring loss; better description, no address book — cannot solve ownership alone |
| more/slower counters | temporal retention, not binding |
| longer positions | irrelevant to fixed-delay binding failure |
| local cache | useful only if its read separately matches keys and returns values |
| dictionary capacity | better features; cannot establish ownership |
| representative slots | strongest repair, MUST have distinct k/q/v roles |
| untied QKV | elevated from ceiling control to possible direct architectural repair |

Representative memory becomes ASYMMETRIC (supersedes the symmetric
rung-3 sketch): q_t = W_q x_t, k_t = W_k x_t, v_t = W_v x_t;
j = route(k_t, K) (binary/WTA prox, constant slots);
V_j = rho V_j + v_t on routed slots; read r_t = sum_j match(q_t, K_j) V_j.
Write-address and read-query are DIFFERENT projections — an actual
associative mechanism at fixed state, no growing cache.

Decision table for the running arms (recorded in advance):
- signed moments pass => squaring was a major culprit. [ALREADY MOOT:
  rung 1 FAILED gate 1 — induction collapsed 1.00 -> 0.55-0.64,
  selective 0.28 -> 0.13, binding lift only +0.04. The beta-read
  interferes destructively with the price-gated read. Verdict:
  screen-DENIED in this form; consistent with "no address book".]
- cache passes only inside 16 tokens => exact recent retrieval works,
  persistent addressing missing => build asymmetric slots.
- untied QKV passes while tied MSSA fails => separate q/k/v roles are
  ESSENTIAL; audit every cache/slot design for that separation.
- representative memory later passes beyond the window => fixed-state
  binding solved.
- untied QKV also fails => stop modifying CRSA; repair the probe
  curriculum/feature block/training setup first.

If QKV passes, the next principled repair is NOT more counter capacity
— it is query-conditioned, asymmetrically written fixed-state memory.


## SWEEP RESULTS, round 1 (2026-08-22, one seed, full grids in
## runs/m4probes/results.jsonl) + the decisive square

| arm | selective mean | binding | induction |
|---|---|---|---|
| kv (tied MSSA, full) | 0.722 | chance everywhere | 1.00 |
| crsa (parent) | 0.283 | at/below chance | 1.00 |
| crsa-sm (rung 1) | 0.132 | ~chance | 0.58 COLLAPSED — gate-1 FAIL |
| cache16 (rung 2, tied local) | **0.881** | ~chance, even in-window | 1.00 |
| qkv (untied, full) | pending FINAL | **1.000 at step 12k, loss 0.001** | pending |

TWO CAPABILITIES CLEANLY SEPARATED: selective retention is SOLVED by
local interaction + recurrent transport (cache16 exceeds even tied full
attention — a restricted local interaction is a better salience-writing
bias than diffuse all-history interaction); arbitrary key->value
binding is untouched by every tied mechanism and PERFECTLY solved by
role separation (untied QKV: 1.000 on every cell where tied sat at
chance). Param caveat: qkv 354k vs 157-222k others — ceiling, not
matched comparison.

Cache16's delay-44/89 selective wins (beyond its window) support:
local cache identifies salience -> representation marked -> counters
transport the mark. OWED CAUSAL CONFIRMATION (write/read intervention):
cache enabled only at encode / only at query / throughout / disabled.
Write-only retaining the delay-89 gain confirms local-write/
global-carry causally.

THE DECISIVE SQUARE (memory extent x role separation):

|  | tied Q=K=V | separate Q/K/V |
|---|---|---|
| full history | binding chance | 1.000 (12k read; FINAL pending) |
| local 16 | binding chance | **cache16-qkv — RUNNING** |

cache16-qkv outcome map (recorded in advance):
- binds inside AND beyond 16 => local role-separated binding converts
  into a counter-transportable feature; representative slots may be
  UNNECESSARY;
- binds inside but not beyond => QKV fixes lookup, counters cannot
  preserve multiple bindings; persistent asymmetric slots justified;
- fails even inside => locality is not the issue; inspect capacity/
  curriculum/implementation;
- (full qkv fails => instrument unusable — already contradicted at 12k).

ROADMAP (narrowed): keep CRSA counters (they own induction); keep the
local pathway (it solved selective); add distinct q/k/v roles ONLY to
the retrieval pathway; add persistent slots ONLY if locally formed
bindings fail to survive beyond the window; LM screen only after
binding passes without damaging induction or selective recall.


## Round-2 protocol (recorded before round-2 arms ran)

QUALIFIED CLAIM (adopted wording): *at matched depth, scale, data and
training budget, every tied-projection mechanism remained at chance,
while separate Q/K/V reached 1.00 across the binding grid* — stronger
and safer than claiming tied projections mathematically incapable.
Full QKV also swept the locked grid (induction 1.00, selective 1.00):
a perfect specialist AND non-destructive under this protocol.

ZERO-GATE OPTIMIZATION INJURY (mathematically real): with
y = y_CRSA + gamma*f_theta(x), dL/dtheta = gamma * dL/dy * df/dtheta —
at gamma=0 every branch gradient is EXACTLY zero; only gamma receives
gradient, driven by essentially random features. Matched-step
comparison: full qkv 0.969 binding at step 4,000; cache16-qkv 0.066 at
the same step, plateauing ~0.11. The current cache16-qkv failure is
therefore NOT yet evidence that local QKV cannot bind — analogous in
spirit to M2's conversion injury. If gamma_0>0 succeeds, classify as
zero-gate injury, not rejection.

Round-2 arms (all chained): cache16-qkv-g01 (gamma_0=0.1, removes the
dead-branch bootstrap) | qkv-w16 (local-only 16-token QKV, no counters:
can a windowed role-separated branch bind inside its window at all?) |
qk-v (Q=K shared, V separate) | q-kv (Q separate, K=V) — the
MINIMUM-UNTYING factorial. Prediction on record (collaborator): Q=K
with separate V may suffice — identity matching benefits from a shared
space; content needs a distinct value channel. If so, main-model cost
falls from +2Ld^2 (~4.82M at d=448 L=12) to +Ld^2 (~2.41M).
Transplant arm (trained qkv weights into the local branch — separates
representational capability from learning) reserved if g01 fails.

PARAMETER/STATE ACCOUNTING CORRECTIONS (on the record):
- Our cache16 implementation gives the local branch its OWN tied U
  (+Ld^2 params: +65k at probe scale, would be +2.41M at d=448 L=12) —
  the zero-extra-matrix description (reusing the CRSA U) is a POSSIBLE
  leaner design, not the one measured. State: Wd values/layer cached
  (~344 KB fp32 across 12 layers at d=448) vs counters' ~21 KB — ~17x
  state increase, still fixed in total context length; 1/16 of a full
  256-token cache.
- Bottleneck arithmetic (correcting an earlier 600k estimate): r=d/4
  costs 4*(d^2/4) = d^2 per layer => ~2.41M at d=448 L=12 — a 4x
  reduction from full-width +9.63M, not 16x. Bottlenecked cache state:
  2Wr (~172 KB).
- Full-width local QKV is a MECHANISM ORACLE, not a viable final LM;
  after mechanism confirmation: decompose tying + bottleneck rank, then
  param-matched CRSA control before any LM-gain claim.

OWED: cache write/read intervention (encode-only/query-only/both/none
at delay 89) to causally confirm local-write/global-carry; gradient
norms + learned gammas from the cache16-qkv checkpoint.


## Round-2 curriculum amendment + g01 verdict (recorded as they landed)

g01 VERDICT: gamma_0=0.1 binding 0.111 (flat, matched g0's 0.119) AND
collateral damage — induction 1.00 -> 0.80-0.91, selective 0.88 -> 0.15.
Open-from-birth random branches interfere without buying the circuit.
Dead-gate initialization is falsified as the sufficient explanation;
THE HYBRID COMPOSITION ITSELF fails to learn the binding pathway.
Transplant arm (trained qkv weights into the local branch) remains the
reserved capability-vs-learning discriminator.

CURRICULUM AMENDMENT (before the reach arm ran): qkv-w16 on the MIXED
stream is not a clean capacity oracle — most binding examples have
store->query distances beyond 16, so a windowed branch trains heavily
on impossible examples (irreducible conflicting gradients). Added:
qkv-w16-reach (binding examples constrained to earliest-store distance
2F+gap <= 15, facts in {2,4}; other tasks unchanged) and two STRICTLY
in-window eval cells (bind-inwin@2: facts 2 gap 6; bind-inwin@4:
facts 4 gap 4) now evaluated for every subsequent arm.

Outcome map (local-only x hybrid, recorded in advance):
| local-only result | hybrid result | interpretation |
|---|---|---|
| passes in-window | hybrid fails in-window | residual-fusion / shared-representation interference |
| passes in-window | hybrid passes in, fails beyond | counters cannot transport multiple bindings; slots justified |
| fails mixed, passes reachable | — | window starved by impossible training examples |
| fails reachable curriculum | — | local QKV circuit inadequate at this scale/config |

Distinction on the record: selective proved counters transport ONE
salience-enriched feature; that does NOT imply transporting several
independently addressable key->value pairs without mixing ownership.
Even perfect in-window binding failing beyond 16 would not contradict
the selective composition result — it would name exactly why
representative slots are needed.


## COUNTERFACTUAL BINDING SUITE — LM-scale results (counterfactual.py,
## locked seed 20260823, 12 cells x 50 counterbalanced twin pairs,
## 7 checkpoints; full grids in runs/counterfactual.json)

VERDICT: NO genuine binding at LM scale in ANY checkpoint. Candidate-
restricted accuracy is at stored-set chance in every cell of every
model (incl. softmax+MLP 12.54 and the 8.08 dictionary model).

THE INSTRUMENT CAUGHT A MASQUERADE: the 8.08 model shows the largest
|S| of the fleet (+0.225 query-first / -0.228 query-last at f2-short,
+0.169/-0.047 at f4-short) — but the SIGN FLIPS with query position.
True binding gives S>0 at BOTH query positions; a prefer-the-FIRST-
MENTIONED-color heuristic gives exactly +/- symmetric S. The best
model learned a PRIMACY heuristic, not assignment tracking. (The f8-
short cells show small S>0 at both positions, +0.073/+0.066 — a
possible whisper of latent signal at chance top-1; n=50/cell, no SE
computed, not a claim.) All other checkpoints: S ~ 0 flat. Distance
kills even the heuristic (long cells ~ 0).

Alignment across scales: binding is absent up and down the stack —
probe-scale tied arms at chance, LM-scale tied models at chance with
position heuristics. The 8.08 model's fluent stories + Billy/Sarah
drift are consistent: perplexity averages thousands of easy
predictions; ownership is guessed from position priors.

Caveats: 3k-screen checkpoints (softmax+MLP, crsa+MLP) are
undertrained relative to the 20k+ models — their null is weaker
evidence; one locked data seed; S standard errors not yet computed;
erased-vs-inaccessible not distinguishable without intermediate-state
probes (recorded as owed).


## MINIMUM UNTYING — RESULT (equal-parameter isolation)

qk-v (Q=K shared, V separate): binding 0.44, degrading with facts
(0.62/0.39/0.23/0.11), induction 1.00, selective 1.00 — partial.
q-kv (Q separate, K=V shared): binding ~1.000 by step 12k (FINAL
pending at write time), same parameter count as qk-v.

EQUAL PARAMS, OPPOSITE OUTCOMES: raw capacity is removed as the
explanation; the FUNCTIONAL ROLE is isolated. The precise claim:
*under this protocol, a distinct query map is sufficient for
near-perfect binding; a distinct value map is not. The bottleneck is
matching a retrieval context to a stored context, not representing
returned content.* Linguistically natural: "Tom has the red ball" and
"Later, Tom picks up his..." express one entity through different
contexts; one projection cannot both encode stored mentions and
formulate later lookups.

THE MINIMAL OPERATOR: k_t = v_t = U^T x_t (one tied memory/content
basis, ONE cached vector per token), q_t = W_q^T x_t (the only new
matrix, +Ld^2 = 2.41M at d=448 L=12), CRSA counters unchanged.

RANK COMPRESSION (m4_rank.py, no training): Delta = W_q - W_kv,
SVD-truncated to r in {0,1,2,4,8,16,32,64,128} at eval. If rank 16-32
retains binding, the repair costs 172-344k params — the white-box
story: one derived memory basis U plus a small query-side
preconditioner translating retrieval contexts into it. Diagonal
preconditioner (dL = 5,376 params) noted as an even smaller control,
possibly too restrictive.

NEXT TARGET (roadmap updated): NOT generic QKV attention — tied
key/value memory + minimally separate query map, first in the
reachable local-cache probe (cache16 with q-kv local branch), only
then at LM scale.


## RANK COMPRESSION — RESULT (m4_rank.py on q-kv-s0, eval-only)

Sharp transition: rank 0-4 of Delta = W_q - W_kv destroys everything
(0.07-0.14 binding; induction/selective also break — the TRAINED
weights depend on the separate query, so rank-0 is not evidence about
tied training); rank 8 = 0.57; **rank 16 = 0.971 binding / 0.967
induction / 0.926 selective; rank 32 = 0.999/1.000/0.994**; rank 64+
= full. Query specialization is intrinsically LOW-RANK: r/d between
1/8 and 1/4 at d=128. Full grids in runs/m4rank.json.

Cost projection at d=448 L=12 (if the ratio transfers — to be
re-measured at scale): rank 32 = 344k params (+2.5% on 14M), rank 16
= 172k (+1.2%) — versus 2.41M full-width query or 9.63M full QKV.
The white-box statement is now measured at probe scale: ONE derived
memory basis U; a small learned query-side preconditioner (rank
~d/8-d/4) translating retrieval contexts into it. q-kv FINAL locked
grid: induction 1.00, selective 1.00, binding 1.000 — the minimal
operator is non-destructive AND ceiling-equal at equal params.


## REACH-ARM VERDICT + RUNG 3 BUILD (gate fired)

qkv-w16-reach: strong local circuits formed (induction 1.00 to delay
44, selective 0.96+ to 22 — window composition reaches ~4W) but
IN-WINDOW BINDING NEVER FORMED (inwin@2 0.36, inwin@4 0.185 — at/below
stored-set chance) despite the reachability curriculum. Outcome-map
row 4 fires. CLAIM KEPT TIGHT: *the tested W=16 operator failed to
learn binding even under the reachable curriculum* — at matched
depth/width/curriculum, full-history role-separated attention binds
perfectly while this windowed configuration does not. NOT a universal
claim that windows kill binding.

Consequence: the constant-state repair must be full-history BY CONTENT.
SlotCRSA built (model.py): M=8 slots, learned slot keys, k=v=U^T x
(tied basis per the minimum-untying result), q = W_q x (the proven
bottleneck matrix), write-routing + read-match via softmax with learned
temperature (v1 deviation: derived WTA prox deferred to hardening),
per-slot dyadic decay ladder m in {3..10}, parallel decayed-cumsum
scan (chunked, causal by construction — verified). State M(d+1)=1,032
values/layer at probe scale; 3,592 at d=448. 227k params at probe
scale. Trained from scratch (no zero-init gate needed at probe scale).
Arm 'slots' queued.


## SlotCRSA judgment protocol (recorded before the arm reports)

Composition caveat standing: the eventual hybrid is assembled from
individually justified components, but their INTERACTION is unproven —
M2 already showed working parts can interfere when composed (and g01
showed it again in-program).

What the M=8 grid can reveal (facts sweep x 8 slots):
| pattern | reading |
|---|---|
| failure before 8 facts | routing or write/read formation defective |
| strong through 8, cliff at 16 | ~one durable binding per slot; collision capacity localized |
| smooth degradation across counts | soft-routing interference, not hard occupancy |
| flat across facts, decays with delay | timescale ladder insufficient |
| flat across delay, degrades with facts | genuine slot-collision signature |
| binding up, induction/selective intact | clean dual-memory success |
| binding up, existing tasks decline | fusion interference persists |

State at LM scale: M(d+1) = 8x449 = 3,592/layer = 43,104 across 12
layers (~172 KB fp32); + counters ~193 KB total, constant in sequence
length, before any local-cache state.

Qualification standing: SOFT routing proves the representative-memory
mechanism, NOT the spike/hardware mechanism. On success, WTA hardening
inherits M2's calibration discipline: measure trained soft assignment
distributions -> calibrate hard thresholds -> shadow soft/hard paths ->
ramp, don't switch -> recheck binding at every transition.

If SlotCRSA passes: first fixed-state associative binding mechanism in
the programme; next step is the M in {4,8,16} sweep for the
state-vs-binding-capacity curve BEFORE any TinyStories run.


## Benefit accounting (collaborator, on the record) + basis ladder

The claim shifts from "448 counters replace attention" to: *a
bounded-memory hybrid retains induction, selective retrieval and
binding with ~0.5 MB of recurrent state independent of sequence
length, versus a KV cache growing throughout the stream* (~20x smaller
at ctx 256, linearly better with length; constant O((W+M)d) per-token
work). Cache-16 as implemented does NOT share U (+2.41M at scale);
SlotCRSA DOES (param count confirms: only W_q + slot keys are new).
If shared-U slots pass binding AND selective at 1.00, the local cache
may be REDUNDANT and the lean final model is: CRSA counters + shared-U
slots + low-rank query correction (~0.2-0.4M params, ~193 KB state).
If slots bind but selective drops, cache16-shareU is the next cheap
ablation. Savings are conditional on slots actually binding.

BASIS LADDER (one rung inserted before own-basis):
| arm | tests |
|---|---|
| slots (shared trainable U) | maximally constrained one-basis hypothesis — RUNNING, negative trajectory at 12k (0.140), finishing to 20k for terminal grid per decision rule |
| slots-frozen (weight-detached U for slot path) | can slots USE the basis without reshaping it? shared-fail/frozen-pass => gradient conflict or moving-basis instability |
| slots-own (independent U_slot) | does binding require its own representation basis? both-shared-fail/own-pass => CRSA's statistical basis lacks discriminable keys; all three fail => slot mechanism itself defective |

Post-hoc diagnostics (m4_slotdiag.py, run on terminal checkpoints):
routing entropy + per-slot occupancy, slot-key geometry, and
cos(g_binding, g_locked) on shared U per layer — negative cosine
supports objective conflict; ~0/positive with flat binding points at
routing/read optimization instead. If slots-own passes, the low-rank
test applies to U_slot = U + Delta_slot as well.


## Shared-U slots — terminal grid + diagnostics (m4_slotdiag.py)

FINAL: binding 0.134 flat; induction PRESERVED (0.955-1.0); selective
mildly lifted (0.32-0.44 vs parent 0.28, far below cache16's 0.88).
Non-destructive, non-binding.

DIAGNOSTICS: routing ALIVE (entropy 1.01/2.08, all slots occupied
0.03-0.34, max slot-key |cos| 0.70) — not an implementation failure.
GRADIENT CONFLICT MEASURED: layer-0 cos(g_binding, g_locked) on shared
U = -0.455 with |g_locked| 2.6x larger — the binding objective loses
the tug-of-war exactly where entity identity must become discriminable;
layers 1-3 mixed (+0.39/+0.05/-0.09), locked dominance up to 7.5x.

Prediction recorded BEFORE the remaining rungs report: frozen should
ALSO fail (the counter-shaped basis never develops discriminable keys;
freezing stops the war but not the shortage), own-basis should PASS if
the slot mechanism is sound. That pattern would conclude: CRSA's
statistical basis does not contain binding keys; slots need their own
basis (then test U_slot = U + Delta low-rank compression).


## SLOTS LADDER — COMPLETE (all grids in results.jsonl) + the v2 diagnosis

| arm | induction | selective | binding vs chance |
|---|---|---|---|
| slots (shared U) | 0.99 | 0.38 | below/at chance, flat |
| slots-frozen | 1.00 | **1.00** | at/below chance (0.23 mean) |
| slots-own | 1.00 | **1.00** | AT CANDIDATE-SET CHANCE almost exactly (facts 0.44/0.22/0.125 vs chance 0.50/0.25/0.125; delay ~0.45-0.52 vs 0.50) |

Outcome-map row 3 fires: ALL THREE basis rungs fail binding — the
mechanism, not the basis, is the problem. The chance-level own-basis
result shows the model learned to answer WITH a stored color but never
WHOSE (bind-mean lifts are chance-inflation). Meanwhile a REAL
component win stands: frozen- and own-basis slots deliver PERFECT
selective retention (1.00, beating cache16's 0.881) at constant
M(d+1) state with no token ring — the local cache is likely REDUNDANT
for selective retention.

V2 DIAGNOSIS (named defect in the v1 design): each token routed to
slots BY ITS OWN CONTENT — "red" routes by red's key, not Tom's — so a
(name -> attr) pair can NEVER co-locate; ownership was
unrepresentable regardless of basis. This is the slot-form of the
two-hop composition attention needed (previous-token + match).
V2 (slots-prev, launched): route each WRITE by the PREVIOUS token's
key so the attribute lands in the slot its owner addressed; read
unchanged (query matches slot keys). One change on the own-basis
config; everything else frozen.


## SLOTS V2 (owner routing) — CLEAN DUAL-MEMORY SUCCESS

slots-prev FINAL (one seed): **induction 1.00 (all delays), selective
1.00 (all delays), binding-mean 0.941**. Per-cell shape matches the
judgment table's success rows EXACTLY:
- delay 0.955-0.985 FLAT across 8-96 tokens => timescale ladder
  sufficient; time does not erode bindings;
- facts 1.00 / 0.95 / 0.91 / **0.615 at 16** => strong through 8,
  cliff at 16 = ~ONE DURABLE BINDING PER SLOT (M=8) — collision
  capacity localized at M, the predicted signature;
- distractors 0.925-0.995 => robust to interfering activity;
- window cells 0.97 in AND out => content-addressable, no positional
  window dependence (the property the reach arm showed windows lack).

THE FIRST FIXED-STATE ASSOCIATIVE BINDING MECHANISM IN THE PROGRAMME.
The single change from chance to 0.94: route each write by the
PREVIOUS token's key (ownership co-location) — the slot-form of
attention's two-hop composition. Config: own U_slot basis, M=8,
soft routing, per-slot dyadic ladder. State M(d+1) per layer,
constant in sequence length.

Standing next steps (per plan): M in {4,8,16} sweep for the
state-vs-binding-capacity curve (prediction now sharp: the facts
cliff should TRACK M); shared-U + owner-routing recheck (v1's
basis verdict is confounded by the v2 defect — the basis ladder must
be re-run on the working mechanism before concluding U cannot serve);
low-rank U_slot = U + Delta compression; WTA hardening via the M2
calibration discipline; only then TinyStories. One seed; replication
owed.

## The drawers statement + what is NOT yet shown (collaborator, adopted)

Plain-language mechanism: eight reusable drawers; "Tom has red" files
red in Tom's drawer (owner-routed write); a Tom-query opens Tom's
drawer (content-addressed read). v1 put Tom and red in different
drawers — ownership structurally impossible.

STRONGEST DEFENSIBLE CLAIM: *on the preregistered synthetic suite,
multiscale counters plus eight owner-routed slots provide
constant-state induction, selective recall, and arbitrary key-value
binding, with degradation governed primarily by memory load rather
than elapsed time.*

NOT YET SHOWN: (1) TinyStories will discover WHEN a token belongs to
an earlier entity — the probe supplies the rule (owner = preceding
token); natural text needs a LEARNED write-key/owner selector;
(2) delay-flat means flat over 8-96, not indefinitely; (3) soft
routing is not a spike/hardware implementation; (4) capacity
interpretation needs the M sweep — and avoid "one total fact per slot"
across 4 layers of slots until the curve confirms; (5) state: slots
~172 KB + counters ~194 KB total fp32 at LM config, bounded in T.

GATES BEFORE TINYSTORIES: (1) capacity cliff moves with M; (2) shared-U
retest under corrected routing; (3) learned owner selector replaces the
previous-token oracle; (4) WTA hardening via M2 calibration; (5) only
then the LM screen. If passed, the coherent LM: counters (statistical
memory) + sparse dictionary (nonlinear features) + few associative
slots (entity binding).

## M SWEEP, first half — M=4 (honest reading)

M=4 FINAL: induction 1.00, selective 1.00, but binding AT STORED-SET
CHANCE everywhere (facts 0.465/0.285/0.10/0.055 vs chance
0.50/0.25/0.125/0.0625; delay ~0.47 vs 0.50). NOT a cliff at 4 facts —
a FORMATION FAILURE: four slots never learned owner-filing at all
(one seed). Candidate explanations: slot contention during circuit
formation; seed variance. The cliff-tracks-M prediction is NOT
confirmed by this cell; M=16 is the discriminating half. M=4 needs a
seed retest before any capacity conclusion cites it.


## M=16 run economics (collaborator, on the record)

The slowness is IMPLEMENTATION, not mechanism: training materializes
B x M x T x d slot state per layer (1.84M values/example/layer at the
LM config before batch and autograd) and scans it through a Python
chunk loop; doubling M doubles that path. Distinctions: (1) INFERENCE
stays bounded — token 10,000 costs what token 100 costs; M=16 slot
state ~344 KB across 12 layers (~366 KB with counters); (2) the
arithmetic is small — slots are O(Md) vs the dictionary's O(d^2); in a
good implementation the dictionary matmuls dominate at d=448.

Treatment: M=16 is a CAPACITY-MECHANISM experiment, not a production
config; M=8 may remain the operating point. Early-stop permitted if
the curve is decisive (facts@16 >> M=8's 0.615). Before any LM-scale
slot run: fused associative scan / compiled kernel, fuse update+read,
checkpoint-recompute instead of retaining M x T x d history — the
FlashAttention lesson applied to recurrent slots (exact operator,
no huge intermediate).


## M SWEEP — COMPLETE (gate 1 CONFIRMED, with one flagged anomaly)

| M | facts 2/4/8/16 | delay | ind/sel |
|---|---|---|---|
| 4 | chance everywhere (formation failure — needs seed retest) | chance | 1.00/1.00 |
| 8 | 1.00/0.95/0.91/**0.615** | flat 0.96-0.99 | 1.00/1.00 |
| 16 | 1.00/1.00/0.995/**0.97** | flat 1.00 | 1.00/1.00 |

The M=8 cliff at 16 facts dissolves when M=16 — binding capacity
TRACKS SLOT COUNT (the one-durable-binding-per-slot reading holds
between M=8 and M=16). The M=4 formation failure is flagged, one seed,
excluded from the capacity claim until retested. M=8 remains the
presumed operating point (quality/state/speed); M=16 was a
capacity-mechanism experiment.

Gates: (1) capacity-tracks-M CONFIRMED (with M=4 caveat) | (2) shared-U
recheck RUNNING | (3) learned owner selector NEXT BUILD | (4) WTA
hardening | (5) TinyStories.


## GATE 3 — offset-varied grammar + learned owner selector (preregistered)

Grammar (m4_probes.make_binding_pat): three store patterns — pre
[name, attr] (the oracle's case), post [attr, OF, name] (owner AFTER
attribute; OF = new token 54), far [name, f1..f_off, attr] (owner
offset+1 back, offsets 1-4). Query unchanged. Gate-3 eval cells:
pat-pre/post/far2 @ facts {2,4} + pat-far4@2. The prev-token oracle
fails post and far BY CONSTRUCTION.

Selector (SlotCRSA.slot_owner_sel): two banded micro-attentions over
the last 8 tokens — sel_a picks the write ADDRESS (whose drawer),
sel_c picks the write CONTENT (what goes in) — replacing both the
t-1 shift and the content=self assumption. +2 d^2/layer at probe
scale; low-rank compression candidate later.

Arms + preregistered predictions:
- slots-prev-pat (oracle on pattern grammar): pat-pre strong;
  pat-post and pat-far AT CHANCE — validates the grammar
  discriminates.
- slots-sel (selector, same grammar): gate PASSES if pre ~ oracle's
  pre AND post/far substantially above chance with induction/selective
  preserved. Selector failing only far4 = window-limited selection
  (extend band); failing everything = ownership selection not
  learnable this way at this scale.

Shared-U recheck meanwhile trending negative at 12k (0.139) —
conflict story holding; own basis (or U + low-rank Delta) stands.


## GATE 2 — CLOSED: shared-U fails under corrected routing

slots-prev-shared FINAL: binding 0.201 (own-basis identical mechanism:
0.941). The v1 gradient-conflict measurement stands as the real story:
the counter objective and the binding objective cannot share one U.
Consequence: slots keep their own basis (+d^2/layer full-width);
mitigation to test: U_slot = U + Delta with low-rank Delta (the query
map compressed to rank 16-32 — the slot basis may too). Gate 3 arms
(oracle-on-patterns control, then learned selector) now running.


## Gate-3 control arm — grammar leakage measured (baseline row locked)

slots-prev-pat (oracle on pattern grammar): pre@2/4 = 0.765/0.405
(DEGRADED from 1.0/0.95 on the pure grammar — conflicting gradients
from unsolvable patterns, the reachability lesson again); post@2 0.75,
post@4 0.305, far2@2 0.56, far2@4 0.245, far4@2 0.615. The post/far
cells LEAK above naive chance at facts=2: the fixed shift files each
value under the PREVIOUS fact's name (in [v OF k] streams, v2's
predecessor is k1), so systematic cross-filing yields exploitable
signal without ownership selection. Induction/selective stayed 1.00.

GATE AMENDMENT (before the selector reports): the selector's pass bar
per cell is the CONTROL ROW above, not naive chance — it must
substantially exceed oracle-exploitation on post/far AND restore
pre toward the pure-grammar level (~0.95). The control run is exactly
why these numbers exist.


## GATE 3, attempt 1 — FAILED (recorded against the control row)

slots-sel: every pattern cell at/below the oracle control (pre@2 0.550
vs 0.765; post@2 0.490 vs 0.75; far4@2 0.490 vs 0.615), all ~
stored-set chance. Induction/selective 1.00 intact. The micro-attention
selector never bootstrapped ownership selection — the third cold-start
failure of the campaign (gamma-gate, slots v1, now selector v1).
DIAGNOSIS BY PATTERN: mechanisms fail when they must discover
structure from nothing and succeed when initialized AT working
structure. Selector v2 (queued): add a learnable position-bias to the
address-selector logits INITIALIZED to prefer t-1 — starts at the
oracle, learns to deviate (the same move that fixed slots v1->v2).

STACKING DECISION (user directive: stack validated pieces, don't
block on replication): integrated LM screen proceeds NOW with the
validated components — counters + overcomplete dictionary + own-basis
slots with prev-token routing as an explicit structural prior (crude
but non-degenerate for English adjacency) + M=8. From scratch, 3k
screen, judged by the LM counterfactual suite (swap sensitivity S at
both query positions), ppl as health check. Selector v2 iterates on
probes in parallel; replication seeds queued behind, overnight.


## INTEGRATED LM SCREEN + counterfactual + replication (overnight)

screen-slots-lm (18.9M: counters + 4d dictionary + M=8 own-basis
prev-routed slots, from scratch, 3k TinyStories): **13.81** — the
CRSA-family screen RECORD (dict 19.75, CRSA+MLP 16.79 at 23.7M).
Param caveat: +4.9M over the dictionary arm. Generation fluent.

COUNTERFACTUAL ON THE SAME CHECKPOINT: S ~ 0 in every cell (max
+-0.03). The binding machinery was NOT recruited for ownership by 3k
steps of language. The predicted risk is now measured: MECHANISM
SOLVED, WRITE POLICY UNLEARNED. The ppl gain is therefore attributed
(provisionally) to the slots' selective-retention capacity, not
binding. Alternatives on record: 3k may be too few steps; prev-token
prior imperfectly matches English possessives.

REPLICATION, slots-prev seed 1: induction 1.00, selective 1.00,
binding 0.712 (seed 0: 0.941; still climbing at schedule end).
The mechanism REPLICATES qualitatively (clean dual-memory, far above
chance); binding magnitude carries real seed variance — quote as a
range (0.71-0.94, n=2) until seed 2 lands.


## Replication complete (3 seeds slots-prev, 2 seeds q-kv) + sub-8

q-kv REPLICATES PERFECTLY: seed 1 = 0.999 binding, induction 1.00,
selective 1.00 (seed 0: 1.000). The query-map localization is solid.

slots-prev across seeds (all: induction 1.00, selective ~1.00):
| seed | facts 2/4/8/16 | delay@8/@96 |
|---|---|---|
| 0 | 1.00/0.95/0.91/0.615 | 0.98/0.955 |
| 1 | 0.87/0.64/0.395/0.18 | 0.88/0.865 |
| 2 | 0.615/0.405/0.24/0.07 | 0.575/0.62 |

The MECHANISM replicates qualitatively (every seed far above chance,
clean dual-memory in all three); binding MAGNITUDE is strongly
seed-dependent (facts@2: 1.00/0.87/0.615). Slot-circuit FORMATION is
stochastic — consistent with the M=4 formation failure and the
cold-start pattern. Claims quote the range; formation reliability
(init/curriculum for the slot pathway) joins the open problems. The
dual-memory preservation result (ind/sel 1.00) replicates at 3/3.

SUB-8 BROKEN: slots-lm-20k at **7.971 by step 11,500** of 20,000 —
first sub-8 constant-state model in the campaign, with 8.5k decay
steps remaining. (Param caveat 18.9M vs 14.0M dictionary stands.)


## SummaryMixing integration (arXiv:2307.07421, collaborator triage)

Not borrowed: the whole-utterance mean (noncausal; uniform statistics
already the losing arm). Borrowed, three priorities:
1. LOCAL BRANCH: causal depthwise conv (k=4, d*k params, shift-register
   hardware) alongside the global counters/slots — their key result is
   cheap-global-works-WHEN-strong-local-exists; QKV currently supplies
   both local alignment and global retrieval, ours mostly global. NOTE
   the probe twist: a k>=2 conv can transport the owner's identity into
   the attribute token — 'slots-conv-self' tests whether conv +
   SELF-routing binds WITHOUT the prev-token oracle (a learned,
   language-plausible write path).
2. GATED FUSION: g = sigmoid(A f(x) + B r) on the slot read
   (instrumentable, quantizable) vs plain residual — small factorial,
   params matched; may also safely revive signed FIRST moments as a
   combiner input (never touching the counter price gate).
3. GROUPED U_slot: block-diagonal (4 groups => d^2/4 = 602k at LM
   scale, vs 2.41M full / 172-344k low-rank) — more expressive per
   group than global low-rank, parallel hardware lanes; hybrid
   blockdiag + rank-8 possible later.
Measurement lesson adopted for the final comparison: report training
s/token vs context, peak memory vs context, recurrent latency at
token 10/100/1k/10k, ppl AND binding beside resources.
All three implemented (model.py), smoked causal; probe arms
slots-conv-self / slots-prev-gated / slots-prev-g4 queued behind the
resumed 20k run.


## THE TRUE REFERENCE (qkv+MLP at matched params) — table complete

| matched ~19M | qkv+MLP (19.4M, d352 L12) | integrated slots (18.9M) |
|---|---|---|
| ppl @3k | 9.32 (36 min) | 13.17 (2h19m) |
| ppl @10k | **6.04** (2h02m) | 8.31 (~7h) |
| inference state | KV cache, grows with T | 194 KB constant |

FIRST MEASUREMENT of the constant-state cost against a TRUE untied
transformer at matched size/budget: ~2.3 ppl at 10k (~0.32 nats/token)
+ 3.5x training-speed deficit (unoptimized slot scan). The prior
tied-softmax reference (12.54@3k) materially understated the ceiling —
untied projections accelerate language circuit formation exactly as
they did probe binding. The slots model's projected 20k final
(~7.4-7.6) will not reach the reference's 10k number. Candidate homes
for the missing 0.32 nats: idle binding machinery (counterfactual S~0),
missing local mixer (conv branch — probe running), optimization.
Schedule note: reference mid-comparisons at its own 10k cosine.


## CONV-SLOTS — GATE 3 PASSED VIA THE LOCAL-ASSEMBLY PATH (one seed)

slots-conv-self (k=4 causal depthwise conv + SELF-routed own-basis
slots — NO oracle, NO selector): induction 1.00, selective 1.00,
binding facts 0.915/0.715/0.455/0.265 (all >> chance), delay-FLAT
0.86-0.92 across 8-96, distractor-robust, window-independent.
facts@2 exceeds oracle seeds 1-2 (0.87/0.615). The write policy is
now LEARNED: the conv assembles owner+attribute composites locally
(whichever pair-member arrives second carries the composite), slots
retain them globally. 1,792 params, shift-register hardware — the
cheapest component in the stack. Divides labor exactly as language
does: pair FORMATION is local, pair RETENTION is global.

Contrast with the failed selector (cold-start micro-attention): the
conv is a direct path (no gate to bootstrap through) with dense local
gradients — consistent with the campaign's cold-start rule.

Follow-ups owed: offset-pattern generalization (post/far grammar);
seeds; conv into the integrated LM (the SummaryMixing local-branch
lesson predicts a ppl gain independent of binding).


## Gated fusion — NEUTRAL (one seed)

slots-prev-gated: facts 0.825/0.675/0.41/0.165, delay-flat, ind/sel
1.00 — inside the additive arm's seed band, +2d^2 params, no gain, no
damage. Additive fusion remains default. The gate's residual case
(consultation logging, quantization, signed-moment port) must be won
at LM scale on ppl if at all. Screen-neutral, not refuted.


## GROUPED BASIS — BEST SLOT ARM OF THE CAMPAIGN (one seed)

slots-prev-g4 (block-diagonal U_slot, 4 groups, d^2/4 params): facts
0.975/0.935/0.845/**0.75 at 16** (dense best seed: 1.0/0.95/0.91/0.615
— the grouped arm even beats the M=8 dense cliff), delay 0.975-1.00
FLAT, distractors 0.96, windows equal, induction/selective 1.00.
CHEAPEST own-basis arm (243k total) outperforms the dense basis
everywhere. Interpretation: per-block gradient density + inter-group
non-interference act as a regularizer (rhymes with low-rank queries
winning). Hardware: four independent narrow lanes.

DESIGN ADOPTED for the next integrated model: grouped-4 slot basis
(602k at LM scale, replacing 2.41M dense) + k=4 causal conv branch
(learned write path + the SummaryMixing local mixer) + additive fusion
(gated was neutral). Caveats: one seed each; conv and grouped not yet
tested TOGETHER (composition rule: one probe first).

SummaryMixing round summary: conv PASS (0.74, learned write path),
gated NEUTRAL, grouped STRONG PASS (0.949). 20k LM run resumed
seamlessly post-SIGSTOP (7.47@14k, curve continuous).


## v2 GAMBLE + PROFILING (collaborator plan executed)

slots-lm-v2 (conv self-routed + grouped-4 basis, 17.1M, fully learned):
**12.33 @3k** — beats v1 (13.17), beats tied-softmax+MLP (12.54 at
23.7M); gap to true qkv reference narrowed 3.85 -> 3.0. PAUSED at 3k
per plan (SIGSTOP; v1-20k also paused at ~14.5k).

TIMING ABLATIONS (d448 L12, batch 16, fwd+bwd): full 2.33 s/step;
slots bypassed 0.37 (slots = 84% of step); conv bypassed 1.94 (conv
0.39 s — MPS depthwise inefficiency, 1.8k params, fixable); slots in
4/12 layers 1.04 (2.2x). Chunk-128 gave only 8%: the cost is the
B*M*T*d tensor traffic, not loop count. Fused update-read kernel spec
recorded (FlashAttention lesson: keep S local, write only y_t,
checkpoint at chunk boundaries, recompute in backward; needs forward
AND gradient equivalence checks). On MPS: try BF16 + compiled paths
before custom Metal.

Architectural speed levers (probing now): layer placement (slots in
1 or 2 of 4 probe layers — if binding survives, 2-3x at LM scale);
then narrow slot values (d/4), WTA single-slot writes (gate 4),
shared bank per stage. Placement factorial running.


## PLACEMENT FACTORIAL — one slot layer suffices (one seed each)

| slot layers (of 4) | facts@2/4/8/16 | delay | ind/sel |
|---|---|---|---|
| 4 (seed band) | 1.00-0.615 @2 | flat | 1.00 |
| 2 (layers 1,3) | 0.745/0.475/0.28/0.105 | 0.70-0.75 | 1.00 |
| 1 (layer 2) | **0.895/0.68/0.30/0.22** | 0.91 FLAT | 1.00 |

1L BEATS 2L and the median 4L seed: explicit associative memory needs
to exist ONCE; counters elsewhere transport; extra slot layers add
interference + cost. LM-scale implication: slots in ~3/12 layers,
projected ~1.2 s/step (from 2.33). v3 design (after v2 completes):
conv everywhere + grouped-4 basis + slots in 3/12 + self-routing.
v2 RESUMED per the carry-on directive (v1-20k remains frozen at 14.5k
as fallback). Caveats: one seed per placement cell; placement
positions not swept (middle layer chosen); formation stochasticity
applies.


## SLOTS-LM-V2 — FINAL: 6.479 (the program's best model)

20k from scratch, 17.1M, fully learned (conv self-routed write path,
grouped-4 basis, M=8, counters, 4d dictionary): **6.479** — 1.6 ppl
below v1's 28.5k-extended best (8.08), 0.44 above the qkv+MLP
reference's 10k final (6.04). Cross-budget note (unlicensed): RWKV's
old 5.5k-schedule number was 6.42. OWED CONTROL: reference at a
matched 20k schedule (its 10k cosine ended early; a 20k reference
would land lower — the honest gap is somewhat larger than 0.44).
Generation fluent. v1 (frozen at 14.5k/7.47) is now superseded; kept
frozen pending a kill/resume decision.

COUNTERFACTUAL AT 20K: the primacy signature STRENGTHENED with quality
(S +/-0.30 by query position at f2, accuracy ~chance) — language
training deepens position heuristics rather than recruiting the
binding machinery, even with the learned conv write path present.
Hypothesis sharpened: TinyStories does not reward ownership enough to
pay the circuit's cost — solvable to ~6.5 without binding. Next
discriminating options: a binding-rewarding auxiliary objective or
corpus (stories with counterfactual-style probes in-distribution), or
accepting the capability as latent (probes prove it exists) and
testing recruitment on a task that needs it (e.g., MIDI voice
tracking, long-form QA).


## Composition probe — conv x grouped INTERFERE on binding

slots-conv-g4 (v2's exact slot design): binding at chance
(facts 0.51/0.25/0.10/0.055), ind/sel 1.00 — far below conv-alone
(0.74) and grouped-alone (0.949). The M2/g01 composition lesson
recurs IN THE SHIPPED DESIGN: v2's ppl win therefore came from
conv-as-local-mixer + slots-as-retention, NOT binding synergy
(consistent with its counterfactual). Mechanistic guess: with the
grouped basis, self-routed composite writes land across group
boundaries incoherently; or the conv path deprives the slot pathway
of gradient. One seed; not diagnosed further yet.

DISCRIMINATING RUN LAUNCHED (user directive): slots-lm-v3g =
grouped-4 + prev-route oracle, NO conv (the 0.949 probe config), 20k.
Decomposes v2's gain at 3k (v2 12.33 / v1 13.17): if v3g ~ v2, conv
was dispensable for ppl; if v3g worse, conv was the ppl driver.
Also ~17% faster/step (no MPS depthwise conv).


## Composition square (conv x slots), completing

| arm | facts@2/4/8/16 | reading |
|---|---|---|
| conv+grouped same-layer (v2 design) | 0.51/0.25/0.10/0.055 | chance — full interference |
| grouped alone (4L) | 0.975/0.935/0.845/0.75 | best |
| dense 1L | 0.895/0.68/0.30/0.22 | strong |
| 1L grouped + block-conv elsewhere | 0.755/0.41/0.265/0.08 | PARTIAL rescue |
| 1L grouped, no conv | RUNNING | the missing control |

Provisional: conv degrades slot binding even from other layers
(placement softens, doesn't cure); the control discriminates conv-cost
from the 1L+grouped pairing cost. v3g stopped at 4.5k (decomposition
served: conv = ppl driver, grouped = ppl-neutral; ckpt kept). All one
seed; formation stochasticity caveat applies throughout.


## Composition square — CLOSED

Control (1L grouped, NO conv) final tracks the bconv arm exactly
(0.502 vs 0.502 mean; twin trajectories throughout). CONCLUSIONS:
(1) block-conv OUTSIDE the slot layer costs binding NOTHING — the
placement-separated conv is binding-free; (2) the deficit vs
grouped-4L (0.949) is the 1L+GROUPED PAIRING — the grouped basis
draws strength from multi-layer redundancy (dense-1L 0.72 > grouped-1L
0.50); (3) same-layer conv+grouped interference stands (0.33).
Design menu (one seed each): max binding = grouped-4L (no same-layer
conv); max speed = 1L (dense or grouped, conv elsewhere free); the
LM ppl champion (v2: same-layer conv+grouped) binds at chance but
TinyStories never pays for binding anyway. Two claims cleanly
separated: LM record on v2's config; the binding architecture claim
on grouped-4L (or conv-elsewhere variants).
