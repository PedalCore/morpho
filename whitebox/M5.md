# M5 — objective-derived fast-weight associative memory (formalization
# before training; adopted 2026-08-24)

DECISION: CRSA + slots is FROZEN as a successful constant-state result
(6.48; binding 0.94 under supervision; full ledger in M4.md). The next
architecture is an implicit online-regression memory — retrieval as
the stated objective — not another counter variant.

## The operator

Each layer maintains a fast-weight matrix S_t (per head, p_k x p_v)
that is the solution of an online ridge regression "given this key,
what value belongs to it?":

  S_t = argmin_S  1/2 ||S k_t - v_t||^2 + 1/(2 b_t) ||S - g_t S_{t-1}||_F^2

Exact closed-form update (delta rule with decay g and write strength b):

  S_t = g_t S_{t-1} + (b_t / (1 + b_t ||k_t||^2)) (v_t - g_t S_{t-1} k_t) k_t^T

Read: y_t = S_t q_t. The residual e_t = v_t - S_{t-1} k_t is the
error-driven write criterion — the principled replacement for slots
guessing what deserves storage. Family: delta-rule / Longhorn
(implicit online regression; Mamba-class training speed) / Gated
DeltaNet (targeted correction + erasure, chunkwise-parallel). Design
decisions inherited FROM M4's measurements: separate query map (the
binding bottleneck; low-rank later), the k=4 causal conv as local
write-association assembler, MLP feature block first (cleanest
control) with the dictionary prox restored in arm E.

## The have-your-cake construction (arm D)

Dense delta state interferes once unrelated associations collide —
fundamental for fixed-size recall (Zoology: associative-recall tokens
= 82% of the recurrent-vs-attention gap). Remedy: delta state for
compressible history + a SMALL BOUNDED EXACT STORE admitting only
high-residual "surprises" (write when ||e_t|| exceeds a calibrated
threshold; bounded slots, LRU/decay eviction). Semiparametric
precedent: HOLA (2025-26 preprint, single-paper, replication owed);
aggressive frontier (NOT first): sparse delta memory via product-key
addressing.

## The ladder (preregistered; one change per rung)

| arm | mixer | tests |
|---|---|---|
| A | QKV + MLP | existing ceiling (6.04 @10k; counterfactual: recency prior, no binding) |
| B | implicit delta (Longhorn-style) + MLP | derived associative state |
| C | gated delta + MLP | targeted erasure/update |
| D | winner + bounded surprise cache | recovery of exact recall |
| E | winner + active dictionary prox | white-box feature block restored |

Full-precision Q/K/V, sigmoid gates, normalization; NO spike
constraints, WTA, shared basis, or low-rank compression until
capability and speed pass.

## Preregistered gates (before any long run)

1. SYNTHETIC: induction + selective ~1.00; binding > 0.9 on the M4
   grid, degrading gracefully with fact count.
2. SPEED: >= 2x faster than the current slots implementation per step,
   ideally within 1.5x of QKV wall-clock — MEASURED AT STEP ZERO
   before any training; published CUDA/Triton speedups do not
   transfer to MPS automatically; if the scan is not efficient here,
   STOP before any 20k run.
3. LANGUAGE: within ~0.3-0.5 ppl of QKV at a matched checkpoint
   before earning a long run.

## Explicitly deprioritized

Another scalar-counter variant (understood: strength and ceiling);
pure Mamba/RWKV as the contribution (controls, not novelty — recall
is their weak point too); TTT-MLP first (expressive, memory-I/O
heavy); interleaved full-attention layers (safe product, weak
science); immediate silicon hardening (earn the operator first).
Mamba-2's lesson: choose a recurrence admitting an efficient
associative scan AND implement the scan properly — the speedup is as
much kernel as architecture.


## Gate 2, first measurement — SEQUENTIAL FORM FAILS AT LM SCALE

The exact sequential delta (256-token python loop, 12 layers, MPS,
fwd+bwd, batch 16) did not complete a 6-step benchmark inside 10
minutes: >> 100x too slow. Per the preregistered rule ("if the scan is
not efficient here, STOP before any 20k run"): NO LM training with the
sequential form. Correctness verified at small scale (causal pre-diff
0). The required engineering before arm B runs at LM scale: a
chunkwise-parallel exact delta (WY representation / DeltaNet-style —
intra-chunk matmul form, inter-chunk recurrence), verified for forward
AND gradient equivalence against the sequential reference. Probe-scale
(d=128) feasibility to be measured separately — the M5 capability
ladder may proceed on probes while the kernel work happens.


## Kernel-reuse verdict + probe-first continuation (collaborator plan,
## adapted to hardware)

FLA (chunked Gated DeltaNet) and the Longhorn kernel are Triton-based:
CUDA/AMD/Intel only — NO MPS backend exists. Per the preregistered
rule (bespoke WY work only if kernels are the contribution): NO custom
kernel here; LM-scale M5 is GATED ON a supported accelerator (cloud
GPU or other machine — user's call) where FLA/Longhorn get benchmarked
at the exact LM shape and verified (forward + gradients + recurrent
state) against the sequential oracle before any training.

LABELING DISCIPLINE (adopted): M5-implicit = the exact proximal
online-regression update (this repo's sequential implementation is the
permanent correctness oracle and the specification for any chunkwise
port). M5-GDN = gated-delta replication/speed control via FLA. Never
silently substituted for each other.

PROBE-SCALE FEASIBILITY MEASURED: 0.25 s/step at d=128 L=4 (fwd+bwd,
batch 16) -> ~1.4 h per 20k probe arm. The capability ladder (gate 1)
proceeds LOCALLY: m5-implicit arm launched on the full M4 grid
(induction/selective/binding + capacity sweep). Gate-1 bar: ind/sel
~1.00, binding > 0.9, graceful degradation with fact count.


## POSITIONING CORRECTION (2026-08-24, on reading Longhorn 2407.14207)

Longhorn derives the recurrent mixer from ESSENTIALLY THE SAME
proximal online-regression objective (their eqs. 5-6): "derive a
sequence mixer from implicit online associative regression" is
LONGHORN'S contribution, not M5's novelty. M5-implicit is hereby
repositioned as: a FULL-MATRIX, EXPLICITLY-DECAYED extension and
diagnostic comparison to Longhorn — not a novel derivation.

THE KEY DIFFERENCE, and the sharpened (potentially publishable)
question: Longhorn derives the full correction (I - eps k k^T) but
IMPLEMENTS the diagonal approximation (1 - eps k^{o2}) because
rank-one-product recurrences resist parallelization while diagonal
recurrences scan efficiently. That approximation is exactly why our
exact sequential form was catastrophically slow — we kept what
Longhorn diagonalized away. QUESTION: does Longhorn's diagonalization
preserve the associative computation implied by its own exact
objective — and if not, can the exact update be parallelized without
surrendering binding? This fits the campaign's method precisely:
locate where a published derivation becomes an approximation, measure
what the approximation costs (with instruments — counterbalanced
binding, selective recall, capacity — that published evals lack;
Longhorn reports strong MQAR and Mamba-competitive LM at 120M-1.3B,
so it is a serious architecture, not adjacent theory).

THE DECISIVE THREE-ARM COMPARISON (matched projections/params/seed/
probe training):
| arm | update | meaning |
|---|---|---|
| M5-exact (running) | full I - eps kk^T + decay | derivation-faithful, slow |
| M5-Longhorn | diagonal 1 - eps k^2 | fast published approximation |
| M5-GDN | gated delta (FLA) | optimized retrieval baseline (GPU-gated) |

Outcome map (recorded in advance): diagonal matches exact on binding
=> use Longhorn's kernel, abandon bespoke exact parallelization;
exact binds where diagonal fails/loses capacity => the approximation's
cost is identified and exact chunkwise parallelization becomes
scientifically justified; GDN matches/wins => learned erasure matters
more than proximal exactness; all pass => choose by LM ppl+throughput.


## Early results + LM-scale reality for the diagonal

M5-implicit (exact, running): **binding 0.991 at step 4,000** (loss
0.002) — the retrieval-derived operator matches full-QKV's trajectory
at constant state, no oracle/slots/routing. Mid-run read; locked grid
at final; 819k-param caveat (largest probe arm).

M5-Longhorn implemented (diagonal 1 - eps k^2, chunked varying-decay
scan, equivalence 1.2e-6 vs sequential diagonal reference, causal).
Probe-scale 0.38 s/step (2.1 h/arm) — queued behind the exact arm.
LM-SCALE HONESTY: even the diagonal form fails LM-scale on MPS in
naive PyTorch — the chunked scan still MATERIALIZES the p x p state
stream (~400 MB/layer before autograd); diagonalization fixes
parallelizability, only a fused kernel fixes materialization. LM-scale
M5 remains GPU-gated FOR BOTH ARMS.


## M5-EAM — the genuinely different arm (collaborator derivation)

Energy-addressed associative memory, built to the parallelization law
(ADDRESSING DEPENDS ON THE TOKEN, NEVER ON MEMORY CONTENTS -> every
slot is a decayed prefix sum -> exact parallel training + recurrent
constant-cost inference): N=64 static learned addresses, conv-formed
owner-aware write keys, sparse top-2 softmax-renormalized writes,
dyadic decay groups, energy read E(j) = -<Qq,C_j> - <Rq,M_j>, and a
LABEL-FREE CONTRASTIVE AUXILIARY (read distribution trained to follow
the write route, CE weight 0.1) — explicit training pressure for
retrieval, the ingredient TinyStories never supplied and the candidate
cure for slot-formation stochasticity. NOT regression: retrieval is
trained against competing addresses. Implemented (850k probe params,
causal verified, 0.44 s/step); queued behind m5-longhorn. Gates:
binding > 0.9; ind/sel preserved; throughput within 1.5x QKV or 2x
slots. Sequential-blockers list recorded (content-dependent routing,
Hopfield settling, online k-means, contents-based eviction) — all
avoided by design. Frontier variant (later): product-key sparse
addressing for N >> 64 at O(kd) compute.


## M5-EXACT — FULL CEILING (one seed)

FINAL: 1.00 in EVERY cell — induction (all delays), selective (all
delays), binding facts 1.0/1.0/1.0/**1.0 at 16** (slots cliffed at
0.615 there), delay-flat, distractor-proof. The exact proximal
online-regression memory matches the untied-QKV ceiling EXACTLY at
constant state (H p^2 = 4,096 values/layer at probe scale) — the
first fixed-memory operator in the campaign to reach the full grid
ceiling, with retrieval-as-objective and nothing else. Loss 0.000 by
12k. Caveats: one seed; 819k params (largest probe arm); sequential
implementation (LM-gated).

The decisive question is now LIVE: m5-longhorn (diagonal 1 - eps k^2)
running — does the published approximation keep this?


## DECISIVE COMPARISON, arms 1-2 — DIAGONAL MATCHES EXACT AT CEILING

m5-longhorn FINAL: 1.00 in every cell (induction, selective, binding
incl. 16 facts, delays, distractors) — identical to m5-exact.
OUTCOME-MAP BRANCH 1 FIRES: the diagonalization (1 - eps k^2)
preserves the associative computation of the exact objective
(I - eps kk^T) at this scale/grid; adopt the published diagonal form
(kernel-supported, scan-friendly); bespoke exact parallelization NOT
justified. The approximation-cost question is answered: ZERO measurable
cost on the counterbalanced binding/selective/capacity grid — a
calibration of a published approximation using instruments its
original evaluations lacked. Caveats: one seed, probe scale (d=128,
16-fact max, delay <= 96); cost could emerge at higher load/scale —
the capacity-stress version (facts >> p?) is the natural follow-up.
m5-eam (contrastive energy arm) now running — the different-objective
comparison completes tonight.


## M5-EAM — CLOSED (honest negative, diagnostics attached)

FINAL: binding 0.067, and induction/selective ALSO at floor (0.04-0.09)
— as sole mixer, EAM bootstraps none of the three capabilities.
DIAGNOSTICS (terminal checkpoint): aux retrieval loss GAMED to ~0
(0.00-0.03 vs chance 4.16) while LM loss stayed at exact chance
(2.77 = ln 16) — route agreement is not value retrieval (diagnostic
row 2). Gaming mechanism: partial occupancy collapse (top-5 addresses
= 64% of writes, entropy 2.42/4.16) with LOW store/query overlap
(0.19): both routing distributions drifted to a degenerate agreement
satisfying the moving pseudo-label without carrying content — the
predicted moving-target failure, resolved by collapse rather than
association. Multiple co-firing diagnostics, no single selected
treatment => closed per the one-rescue rule, no rescue spent.

VERDICT SENTENCE (for the paper): *the energy-addressed contrastive
memory is parallel and non-regressive in algebra, but not reachable
from random initialization under a self-generated route-alignment
objective; its aux loss is satisfiable by routing collapse without
value retrieval.* Fourth member of the cold-start family, with the
sharpest mechanism yet.

## M5 NIGHT — LADDER VERDICT

| arm | ind | sel | binding (incl. 16 facts) |
|---|---|---|---|
| M5-exact (regression, full) | 1.00 | 1.00 | 1.00 everywhere |
| M5-Longhorn (regression, diagonal) | 1.00 | 1.00 | 1.00 everywhere |
| M5-EAM (contrastive energy) | 0.06 | 0.06 | 0.07 — closed |

Regression memories win DECISIVELY: retrieval-as-objective with a
closed-form update bootstraps everything; the contrastive alternative
bootstraps nothing. THE OPERATOR GOING FORWARD: Longhorn-form
diagonal (zero measured approximation cost, kernel-supported).
Next: arms D (bounded surprise cache) and E (dictionary block) compose
on the diagonal base at probe scale; LM-scale + gate 2 + FLA baselines
on the first GPU session; capacity-stress grid (facts approaching p)
as the follow-up instrument; hardware costing of the p x p delta state
(the 60-gate counter story does NOT transfer — separate pass owed).
One seed throughout tonight; replication owed on the two winners.


## Stress protocol (preregistered before any stress arm ran) + D/E gating

Arms D/E DEFERRED with reason: the diagonal is at ceiling on the
existing grid — arm D (surprise cache) would be uninformative until
the operator's failure surface is mapped. Vocabulary caps facts at 16,
so capacity stress INVERTS THE RATIO: shrink per-head state instead of
growing facts — p=16 (8 heads) makes 16 facts = 100% loading.

Stress grid (eval_sets_stress): facts {8,12,16} + OVERWRITE cells
(make_update — re-bind keys to new values; correct answer is the NEW
value; rebinds {1,2,4} + a far variant) — update fidelity, an axis the
original grid never tested. Update task added to the stress training
stream (50% of binding examples) so overwrite is in-distribution.

Running: m5-lh-p32-stress | m5-lh-p16-stress | m5-longhorn seed 1
(replication of the operator decision). Readings: p16 degradation
localizes capacity; update cells test one-owner-one-value fidelity
(superposition would fail them); if the diagonal breaks anywhere,
arm D gets its measured gap. Arm E remains LM-scale/GPU work.


## STRESS + REPLICATION — COMPLETE. No failure surface found.

Replication: m5-longhorn seed 1 = 1.00 everywhere (2/2 seeds, ZERO
variance — the slot family's formation stochasticity does not exist
for regression memory).

Stress: p32 arm = 1.00 on every stress cell including ALL FOUR
OVERWRITE cells (rebinds 1/2/4 + far) — the delta memory REPLACES
bindings rather than superposing (the fidelity axis nothing prior was
tested on, and exactly what the delta rule's error-driven update is
derived to do). p16 arm (16 facts = 100% per-head state loading):
1.00 across the grid — full loading does not crack it.

CONSEQUENCES: (1) arm D (surprise cache) RETIRES UNNEEDED — no
measured gap exists for it to fill within this vocabulary's reach;
(2) the operator decision (Longhorn-form diagonal) now rests on
replicated, stress-tested ground; (3) the remaining unknowns are
LM-scale (GPU session: gate 2, gate 3, FLA baselines, arm E feature
block) and true capacity limits beyond the 16-name vocabulary (a
larger-vocab stress protocol if ever needed). M5 probe phase CLOSED.
